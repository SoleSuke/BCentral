package com.project.task.model;

import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import java.sql.Timestamp;

import org.apache.kafka.clients.admin.NewTopic;

import lombok.Data;

@Data
@Configuration

public class  extends Timestamp{
	public msjJSON(long time) {
		super(time);
		this.id=GenerationType.IDENTITY;
		this.myTimeStamp=this.getTime();
		this.message="This is the Constructor message";
		this.<source type> = { "app0" "type0" };
		// TODO Auto-generated constructor stub
	}
	private static final long serialVersionUID = 1L; // No se xq se agrego esto
	private Long id;
	private Timestamp myTimeStamp(this.getTime());
	private String message;
	private <String String> source type;
}
// No se xq esto se agrego arriba